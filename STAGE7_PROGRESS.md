# Этап 7 — ход работ

План — `STAGE7_PLAN.md`. Начат 24 сентября 2026 года после допуска по `PRE_STAGE7_STABILIZATION_PROGRESS.md`. Порядок — по плану: 7.1 → 7.2 → 7.3 → 7.4 → 7.5.

Живые проверки — `scripts/live-excel-stage7.mjs` (Excel для Windows, Office 16.0.14334, ExcelApi 1.14; книга — копия эталона `pivot-s2-run.xlsx`, лист `Э7`; «модель» отвечает скриптом готовым вызовом, карточка подтверждается в панели). Отдельно отмечается, где проверка шла на настоящей модели.

| Пункт | Статус |
| --- | --- |
| 7.1.1 один активный запрос | сделан на стабилизации (S5) |
| 7.1.2 шаблон формул по столбцам | выполнен, проверен в Excel |
| 7.1.3 формулы как результат по умолчанию | выполнен, проверен на DeepSeek в Excel |
| 7.1.4 ошибки после записи | выполнен, проверен в Excel; дефект показан на прежней сборке |
| 7.1.5 доступность функций | выполнен, проверен в Excel; дефект показан на прежней сборке |
| 7.1.6 проверка набора API | выполнен; в Excel инструменты выдаются по-прежнему |
| 7.2.1 профиль данных | выполнен, проверен в Excel |
| 7.2.2 лишние пробелы | выполнен, проверен в Excel |
| 7.2.3 числа и даты из текста | выполнен, проверен в Excel |
| 7.2.4 дубликаты | выполнен, проверен в Excel |
| 7.2.5 результат на новый лист | выполнен, проверен в Excel |
| 7.2 на настоящей модели | DeepSeek: очистка обычными словами — прошла |
| 7.3.1 переименование и удаление листа | выполнен, проверен в Excel |
| 7.3.1 копирование листа | не выдано: `worksheet.copy` падает на этой машине (ниже) |
| 7.3.2 вставка и удаление столбцов | выполнен, проверен в Excel |
| 7.3.3 группировка | выполнен, проверен в Excel |
| 7.3.4 сводная на новом листе | выполнен, проверен в Excel |
| 7.4.2 правила проверки ввода | выполнен, проверен в Excel |

## Замер до работ: ошибки и функции этого Excel

24 сентября 2026 года в Excel записаны формулы с заведомыми ошибками и функциями разных версий, прочитаны `formulas`, `values`, `valueTypes`.

**Ошибки Excel отдаёт на языке интерфейса:** `#ДЕЛ/0!`, `#Н/Д`, `#ИМЯ?`, `#ЗНАЧ!`, `#ЧИСЛО!`, `#ПУСТО!`, `#ССЫЛКА!`; `valueTypes` у всех — `Error`. Поиск новых ошибок после записи (`formulaErrors` в `excelTools.ts`) сравнивает с английскими названиями — `#DIV/0!`, `#NAME?` и т. д., — поэтому **на русском Excel `newErrors` после записи всегда пуст**. Это дефект пункта 7.1.4; признак, не зависящий от языка, — `valueTypes = Error`.

**Функции этого Excel** (Office 2021, сборка 16.0.14334): работают `XLOOKUP`, `XMATCH`, `FILTER`, `SORTBY`, `UNIQUE`, `SEQUENCE`, `LET`, `TEXTJOIN`, `IFS`, `CONCAT`; дают `#ИМЯ?` — `LAMBDA`, `TEXTSPLIT`, `VSTACK`, `TAKE`, `IMAGE`, `GROUPBY`. Набор функций зависит от версии Excel у пользователя, и формула, верная для одного Excel, в другом даёт `#ИМЯ?` — отсюда пункт 7.1.5.

## 7.1.2. Шаблон формул по столбцам

Выполнено 24 сентября 2026 года.

`fill_range` принимал одну формулу или одно значение на всю область. Для таблицы с разными формулами по столбцам — сумма, налог с неё, статус — модели оставалось звать заполнение по разу на столбец или перечислять ячейки через `set_range_values`.

**Сделано.** Новый параметр `template` — первая строка области, по элементу на столбец: формулы (с `=`) и значения вперемешку. Путь тот же, что у одной формулы после стабилизации S1: первая строка пишется как есть, читается в виде R1C1, и эта строка R1C1 пишется на все строки области — ссылки подстраивает Excel, своего разбора формул нет. Сверяется каждая ячейка по R1C1 своего столбца; `#ССЫЛКА!` от ушедшей за край ссылки называется отдельно; если запись области не прошла, первая строка возвращается как была (`failed_before_write`). Передаётся или `value`, или `template`; шаблон не той ширины отклоняется до записи. Карточка показывает шаблон первой строки.

Протяжка вправо шаблоном-столбцом не делалась: в плане речь о «протяжке по строкам», и примеров, где она нужна, пока нет.

**Проверки.** В `fillRange.test.ts` 6 новых тестов (три формулы по столбцам, имена листов и столбцы таблиц в шаблоне, столбец значений рядом с формулой, отказ по ширине и по сочетанию `value`/`template`, пропущенная Excel ячейка шаблона, возврат первой строки при отказе); на прежнем коде упали все 6. `npm run check`: 301 клиентский, 46 серверных, сборка.

**В Excel** (сборка `2026-09-24 00:33`):

```
прошла  7.1.2 шаблон из трёх формул протянут вниз по своим столбцам
        executionState: verified
        E2:G2 = =C2*D2 | =E2*0.2 | =IF(B2="Новая","в работу","архив")  → 2700 | 540 | в работу
        E7:G7 = =C7*D7 | =E7*0.2 | =IF(B7="Новая","в работу","архив")  → 2600 | 520 | архив
```

## 7.1.4. Ошибки после записи

Выполнено 24 сентября 2026 года.

**Дефект** (выявлен замером выше): новые ошибки после записи искались по английским подписям, и на русском Excel `newErrors` всегда был пуст; заполнение ловило только `#ССЫЛКА!`.

**Сделано.** Ошибка определяется по `valueTypes = Error` — признаку, не зависящему от языка; вид ошибки — по подписям на русском и английском (`functionProbe.ts`: `errorCells`, `errorKind`). Запись и заполнение называют новые ошибки с адресами (`newErrors: ["E3 #ДЕЛ/0!"]`) и пояснением, что значит каждая (`errorNote`); исход остаётся `verified` — записано ровно то, что просили, — но модель обязана назвать ошибку пользователю (правило в инструкции).

## 7.1.5. Доступность функций

Выполнено 24 сентября 2026 года.

**Способ выбран замером.** Писать во временную ячейку всю формулу нельзя: ссылка вроде `=SUM(1:1)` захватит саму ячейку, и Excel покажет окно о циклической ссылке. Замер показал другой признак без ссылок: запись `=ИМЯ()` существующей функции Excel либо отклоняет (`InvalidArgument` — мало аргументов), либо вычисляет; несуществующей — записывает и даёт `#ИМЯ?`. Так же ведут себя русские имена функций (`СУММ`) — через API они не понимаются.

**Сделано.** При подготовке записи или заполнения формулами панель выделяет имена функций (текст в кавычках и имена листов пропускаются). Функции, уже известные как недоступные, отклоняются сразу — до карточки. Для непроверенных выбирается пустая ячейка через столбец правее и данных, и цели; карточка говорит, какие функции и где будут проверены. При исполнении, до записи цели, каждая функция проверяется `=ИМЯ()` в этой ячейке, ячейка очищается и перечитывается; ответ запоминается до конца сеанса. Недоступная функция — `failed_before_write`, цель не тронута, модели сказано назвать ограничение и предложить замену явно. Если ячейка занята или места нет — функции честно помечены «не проверены», и тогда `#ИМЯ?` после записи назовёт 7.1.4. Не очистившаяся ячейка — `unknown` с адресом.

Ограничение: проверяется имя функции, а не число и типы аргументов; неизвестные именованные диапазоны (тоже `#ИМЯ?`) до записи не ловятся — их называет 7.1.4 после записи.

## 7.1.3. Формулы как результат по умолчанию

Выполнено 24 сентября 2026 года. В инструкцию модели добавлены правила: «посчитай и запиши» — формулами; значения — только по прямой просьбе; своё число называть своим расчётом, результатом Excel — только возвращённое инструментом; имена функций — по-английски; недоступную функцию не подменять молча; новые ошибки называть.

## 7.1.6. Проверка набора API

Выполнено 24 сентября 2026 года. Прежде набор ExcelApi проверялся у четырёх инструментов по именам, остальные выдавались модели без проверки. Теперь `MIN_EXCEL_API` в `toolSchemas.ts` объявляет набор у каждого инструмента — по тому, что используется без запасного пути; тест требует его у всех и не выше 1.14, второй тест проверяет, что на Excel с 1.7 сводная и фильтр не выдаются. Два файла тестов, где Office не был задан, получили подставной Excel с 1.14 — ожидания тестов не менялись.

## Проверки 7.1.3–7.1.6

**Автоматические.** `functionProbe.test.ts` — 7 тестов модуля; в `fillRange.test.ts` — 5 тестов встроенной проверки (недоступная функция и её запоминание, русское имя, доступные функции проверяются один раз, `#ДЕЛ/0!` в заполнении и в обычной записи); на прежнем `excelTools.ts` упали все 5. `toolSchemas.test.ts` — 2 теста набора API. `npm run check`: 315 клиентских, 46 серверных, сборка.

**В Excel** (сборка `2026-09-24 00:51`):

```
прошла  7.1.2 шаблон из трёх формул протянут вниз по своим столбцам
прошла  7.1.5 недоступная функция: отказ до записи, цель и временная ячейка чисты
        failed_before_write; «В этом Excel нет функций TEXTSPLIT: формула с ними дала бы #ИМЯ?…»; F2 осталась «было»
прошла  7.1.5 та же функция второй раз — отказ сразу, без карточки
прошла  7.1.5 доступная функция проверена и записана
        functionsChecked: {"available":["XLOOKUP"]}; E2:E7 = 900…1300
прошла  7.1.5 русское имя функции поймано так же          «нет функций СУММ»
прошла  7.1.4 #ДЕЛ/0! после записи назван
        newErrors: ["E3 #ДЕЛ/0!"]; «#ДЕЛ/0! — деление на ноль в данных…»
```

На прежней сборке (`2026-09-24 00:33`, выпуск временно переключён и возвращён) — 0 из 5: формула с `TEXTSPLIT` записана и получила **`verified`**, хотя в ячейках `#ИМЯ?`; `=СУММ(...)` — тоже `verified`; `#ДЕЛ/0!` не назван (`newErrors` нет). После возврата новой сборки прогон сводных S3.1 (набор 1.8) — 4 из 4: инструменты со старшим набором выдаются.

**На настоящей модели** — DeepSeek `deepseek-flash`, `scripts/live-excel-model.mjs`, просьбы 10–13:

| № | Просьба | Что в книге | Итог |
| --- | --- | --- | --- |
| 10 | В F1 запиши общую сумму всех заказов | `=SUM(C2:C13)` → 11000 | формула |
| 11 | Посчитай среднюю сумму по Казани и запиши рядом | подпись в F1, `=AVERAGEIF(A2:A13,"Казань",C2:C13)` → 537,5 | формула |
| 12 | Добавь столбец «Доля» | 12 формул `=C2/SUM($C$2:$C$13)`, процентный формат | формулы |
| 13 | Запиши в F1 общую сумму значением, без формулы | 11000 числом | значение; модель сама назвала его «мой расчёт, Excel тут ничего не считал» |

Прогон один на каждую просьбу; до изменения инструкции DeepSeek тоже писал формулы (приёмка S7, просьбы 2 и 7), так что правило закрепляет поведение, а не создаёт его, — вывод о его влиянии по этим прогонам не делается.

## 7.2.1. Профиль данных

Выполнено 24 сентября 2026 года.

**Замер до работ.** Число или дата, записанные текстом, остаются в Excel текстом (`valueTypes = String`), в том числе с обычным или неразрывным пробелом внутри: `1 200`, `01.02.2026` в расчёт не входят. Разделители книги Excel сообщает сам (`application.cultureInfo`, ExcelApi 1.12): ru-RU — десятичная запятая, пробел (0x20) между тысячами, дата `ДД.ММ.ГГГГ`. Коды числовых форматов API принимает и отдаёт по-английски: `dd.mm.yyyy`, `#,##0.00`; русский вариант `# ##0,00` Excel истолковал неверно и показал `1 2 34 568` — это вписано в правила, когда дойдёт до форматов.

**Сделано.** Новый инструмент только для чтения `profile_range` (`dataCleaning.ts`, чистая логика — `dataProfile.ts`). По каждому столбцу — типы значений, пустоты, формулы, число разных значений и находки с тремя примерами: пробелы по краям, двойные пробелы внутри, неразрывные пробелы, числа текстом, числа по чужим разделителям (`1.5` в русской книге), неоднозначные числа (`1,500` — полтора или тысяча пятьсот), коды с ведущими нулями (`007` — не число), даты текстом и неоднозначные даты (`01.02.2026` — 1 февраля или 2 января). По строкам — пустые строки, полные дубликаты и кандидаты в дубликаты после обрезки пробелов и без учёта регистра. Неоднозначное не угадывается и считается отдельно; двузначный год датой не считается. Таблица читается порциями по строкам, до 100 000 ячеек; ответ называет проверенную область и `incomplete`. Содержимое таблицы в ответ не выгружается — только счётчики и примеры. Набор API — 1.12 (разделители книги).

**Проверки.** `dataProfile.test.ts` — 6 тестов (разделители, неоднозначные числа и даты, порядок даты книги, коды форматов дат, профиль таблицы, дубликаты-кандидаты). `npm run check`: 315 клиентских (с новым файлом — 321), 46 серверных, сборка.

**В Excel** (сборка `2026-09-24 05:40`, лист `Э7Ч` с набором типичных проблем выгрузки): все 13 ожидаемых находок совпали, карточек нет, культура `ru-RU`, `DMY`:

```
A «Город»: edgeSpaces 2 (A2 « Москва», A5 «Омск »), innerSpaces 1 (A4 «Казань  Север»), nonBreakingSpaces 1
B «Сумма»: numbersAsText 2 (1 200; 2 300,50), foreignNumbersAsText 1 (1.5), ambiguousNumbersAsText 1 (1,500)
C «Дата»:  datesAsText 2 (25.02.2026; 2026-03-05), ambiguousDatesAsText 2 (01.02.2026), dates 1
D «Код»:   codesWithLeadingZeros 1 (007), numbersAsText 4
строки:    emptyRows 1, duplicateRows 1 (строка 7 = строка 3)
```

## 7.2.2–7.2.3. Лишние пробелы; числа и даты из текста

Выполнено 24 сентября 2026 года.

**Сделано.** Два изменяющих инструмента на общем исполнителе (`dataCleaning.ts`, решения по ячейкам — `cleanModel.ts`):

- `trim_text` — пробелы по краям, неразрывные пробелы и (по умолчанию) повторы внутри, как функция TRIM. Текст пишется с апострофом: без него Excel превратил бы очищенное «007» или «1200» в число.
- `convert_values` — текст в числа (`to: "number"`) или даты (`to: "date"`). По умолчанию меняются только однозначные значения по разделителям книги; `decimalSeparator` и `dateOrder` — только если их назвал пользователь (правило в инструкции модели). Коды с ведущими нулями не превращаются в числа даже по прямой просьбе. Дата получает порядковый номер и формат даты книги (`dd.mm.yyyy` по шаблону `ДД.ММ.ГГГГ`) — предпросмотр прямо говорит, что меняется и значение, и отображение.

Подготовка решает судьбу каждой ячейки: изменить — и на что, или пропустить — и почему (формула; не текст; неоднозначно: дробь или тысячи; разделитель не как в книге; код с нулями; день и месяц не различить; противоречит названному порядку). Карточка показывает число изменяемых ячеек, до восьми пар «было → станет» и пропуски с причинами и примерами; если менять нечего — отказ до карточки. Исполнение: книга та же, область не менялась после предпросмотра (`failed_before_write`), пишутся только изменяемые ячейки порциями по 2000, затем каждая перечитывается — значение, тип, а у дат и вид: книга с системой дат 1904 дала бы другую дату, и это ловится по отображению. Ячейки вне плана сверяются на неизменность. Отмена возвращает содержимое (текст — текстом) и у дат прежний формат. Предел — 20 000 ячеек за операцию. Наборы API: `trim_text` — 1.2, `convert_values` — 1.12.

**Проверки.** `cleanModel.test.ts` — 4 теста (TRIM, числа с однозначными и названными разделителями, даты с порядком и без, порядковый номер 46054 = 01.02.2026 и коды форматов — по замеру); `cleanRange.test.ts` — 9 тестов на исполнитель (код остаётся текстом; неоднозначное не тронуто; названный разделитель; дата и формат; книга 1904 ловится по виду; правка после предпросмотра; пропущенная Excel ячейка; отмена возвращает текст и формат; «менять нечего» до карточки). Защитные тесты реестра сработали, как задумано: пока новые инструменты не были допущены к записи, выдача упала. `npm run check`: 334 клиентских, 46 серверных, сборка.

**В Excel** (сборка `2026-09-24 05:49`, лист `Э7Ч`):

```
прошла  7.2.2 лишние пробелы убраны, текст остался текстом
        изменено 3; A2:A8 = Москва, Москва, Казань Север, Омск, —, Москва, Омск — все String; коды D2:D8 — «менять нечего»
прошла  7.2.3 числа из текста — только однозначные, по разделителям книги
        B2:B8 = 1200, 900, «1,500», «1.5», —, 900, 2300,5; пропущено с причинами: 1,500 (дробь или тысячи), 1.5 (разделитель не как в книге)
прошла  7.2.3 названный пользователем разделитель: 1,500 → 1500, 1.5 → 1,5
прошла  7.2.3 даты из текста — однозначные получают дату и формат книги, неоднозначные ждут
        25.02.2026 → 46078, 2026-03-05 → 46086 («05.03.2026»), формат dd.mm.yyyy; 01.02.2026 осталась текстом
прошла  7.2.3 названный порядок DMY: 01.02.2026 → 1 февраля (46054)
прошла  7.2.3 отмена вернула текст и прежний формат           C3 = «01.02.2026» (String), General
```

## 7.2.4. Дубликаты

Выполнено 24 сентября 2026 года.

**Замер до работ** (`removeDuplicates`, ExcelApi 1.9, на таблице из 11 строк с ключом «Город»+«Сумма»): регистр не различается («москва» удалена как повтор «Москва»), пробел в конце различается («Москва » осталась), текст «1» и число 1 — разные значения (у сводной было наоборот — у неё свои правила), пустые строки — повторы друг друга, остаётся первое вхождение. Удалено 4, осталось 7. И две опасности: сдвигаются только ячейки области — соседний столбец не сдвинулся, строки разъехались с ним; ссылки не подстраиваются — `=C8` осталась `=C8`, но показывает уже другую строку, без всякой ошибки.

**Сделано.** `remove_duplicates` (`dataCleaning.ts`; расчёт — `planDuplicates` в `cleanModel.ts` по правилам замера). Подготовка заранее считает, какие строки уйдут и какой станет область, и отказывает до карточки, если в области формулы, если она задевает таблицу Excel, если вплотную слева или справа есть данные (строки разъехались бы), если ключевого столбца нет (перечисляет настоящие заголовки) или дубликатов нет — тогда с подсказкой, если они появились бы после `trim_text`. Предпросмотр: ключ, число удаляемых строк, до восьми строк «строка N = строка M: значения», формулы книги, которые станут смотреть на другие строки (`shiftedReferences` в `rowOps.ts`: разбор ссылок дополнен столбцами; итог по всей высоте области не считается задетым), непроверенные большие листы, отсутствие отмены и последняя резервная копия. Исполнение: книга та же, область и её соседи не менялись, затем `removeDuplicates` и построчная сверка области с расчётом; другое число удалённых или другая строка — `applied`. Отмены нет, и инструкция модели требует сначала предложить резервную копию.

**Проверки.** `cleanModel.test.ts` — тест на замеренный набор: расчёт панели совпал с Excel (4 удалено, 7 осталось, тот же порядок). `cleanRange.test.ts` — 5 тестов: удаление и построчная сверка, названная формула `=C3` при незадетой `=SUM(C2:C7)`; Excel, сравнивший текст с учётом регистра, пойман (`applied`); соседние данные, формулы в области, таблица, неизвестный столбец — отказ до карточки; «дубликатов нет» с подсказкой про пробелы. `npm run check`: 335 клиентских (с новыми тестами — 340), 46 серверных, сборка.

**В Excel** (сборка `2026-09-24 05:54`, лист `Э7Д` — таблица из замера и формулы `=C8`, `=SUM(C2:C12)` в стороне):

```
прошла  7.2.4 дубликаты удалены так, как рассчитала панель; задетые формулы названы
        verified; удалено 4, осталось 7; номера после: 1, 3, 4, 5, 6, 8, 10, —, —, —, —
        задетые формулы: Э7Д!F2 =C8 (=SUM(C2:C12) не названа — итог по всей высоте)
прошла  7.2.4 данные вплотную к таблице — отказ до карточки
        «…есть данные в столбцах D: … строки разъехались бы с соседними столбцами. Расширьте область на всю таблицу.»
```

## 7.2.5. Результат на отдельный лист

Выполнено 24 сентября 2026 года. Из операций очистки без отмены — только удаление дубликатов, поэтому вариант «без разрушения источника» сделан для него: параметр `destSheet` — существующий пустой лист (создаётся через `create_sheet`). Источник не меняется: уникальные строки с шапкой копируются туда значениями (формулы источника — их результатами, текст — текстом), копия сверяется построчно, и у неё есть отмена — очистка копии. Проверки соседних данных, формул в области и таблиц в этом режиме не нужны: источник не сдвигается. Отказ до карточки, если листа нет, он тот же, что источник, или не пуст. Инструкция модели требует перед удалением дубликатов предложить пользователю выбор: резервная копия книги или отдельный лист.

**Проверки.** `cleanRange.test.ts` — 2 теста (копия при соседних данных и формулах в источнике, источник не тронут, отмена очистила копию; отсутствующий, тот же и непустой лист — отказ). `npm run check`: 342 клиентских, 46 серверных, сборка.

**В Excel** (сборка `2026-09-24 06:01`):

```
прошла  7.2.5 уникальные строки — на отдельный лист, источник не тронут
        источник Э7Д!A1:D12 не изменился (с заметкой в D5 рядом); копия: Номер 1, 3, 4, 5, 6, 8, 10; «1» осталась текстом
прошла  7.2.5 отмена убрала копию
```

## 7.2 на настоящей модели

DeepSeek `deepseek-flash`, `scripts/live-excel-model.mjs`, просьба 14 — лист `П14` с пробелами, числами и датами текстом, неоднозначными `1,500` и `01.02.2026`, кодом `007` и тремя одинаковыми строками: «почисти данные: убери лишние пробелы, преврати числа и даты, записанные текстом, в настоящие и убери повторяющиеся строки».

Модель начала с `profile_range`, убрала пробелы (`trim_text`), преобразовала однозначные числа и даты (`convert_values`) и остановилась с тремя вопросами — ровно теми, которые требует план: порядок дат, что значит `1,500`, и как убрать повторы, раз отмены нет, — резервная копия или отдельный лист. После ответа пользователя («день.месяц.год; 1,500 — это 1500; повторы — на отдельный лист „П14 чисто“, исходные не трогай») создала лист, скопировала туда уникальные строки и отчиталась. Итог в книге: пробелов по краям нет, все суммы и даты — числа (даты с форматом `dd.mm.yyyy`), на «П14 чисто» — 6 уникальных строк, источник со всеми строками на месте, прочие листы не тронуты. 15 вызовов, 9 карточек, 13 запросов к модели, 62 с, 279 тыс. токенов.

Замечено:

- Модель превратила в числа и столбец «Код» («12», «45» — коды), оставив текстом только «007» с ведущим нулём. Просьба «числа, записанные текстом» это формально допускает, но код с виду — не число. Панель такие значения отличить не может; кандидат на правило инструкции: перед преобразованием столбца, похожего на идентификаторы, спросить.
- `1,500` модель записала числом 1500 через `set_range_values`, а не через `convert_values` с названным разделителем. Результат верный, но это число от модели, а не от преобразования панели.
- Очистка дорогая по токенам: каждая операция — отдельный шаг с полной историей.

## 7.3.1. Переименование и удаление листа

Выполнено 24 сентября 2026 года.

**Замер до работ.** Переименование сохраняет ID листа; обычные ссылки (`=Лист!A1`, `SUM(Лист!A1:A2)`) и именованные диапазоны Excel переписывает сам, значения не меняются. Имя внутри текста — `INDIRECT("Лист!A1")` — Excel не трогает, и формула становится `#ССЫЛКА!`. Удаление превращает все ссылки на лист в `#REF!`, именованный диапазон на нём — в `=#ССЫЛКА!$A$2`, и формулы, которые им пользуются, — тоже. Монитор изменений при переименовании историю отмены не чистит (событие имени лишь отмечает изменение структуры), при удалении — чистит.

**Сделано** (`sheetPlans.ts`, разбор — `sheetImpact.ts`):

- `rename_sheet`. Имя проверяется правилами Excel (занятое — отказ со свободным вариантом). Предпросмотр: сколько формул Excel перепишет сам; формулы с прежним именем внутри текста (INDIRECT, HYPERLINK) — сломаются; ячейки, где имя упомянуто просто текстом, — не изменятся; непроверенные большие листы. Исполнение: тот же лист по ID ещё с прежним именем, новое имя свободно, затем переименование и сверка — у листа с тем же ID новое имя, а значения всех формул, ссылавшихся на него, прежние. Отмена — обратное переименование, если лист ещё носит новое имя и прежнее не занято. Отмена устроена стопкой, поэтому переименование, сделанное агентом, отменяется раньше записей на этом листе и возвращает имя, по которому они его найдут.
- `delete_sheet`. Последний видимый лист — отказ до карточки; защищённая структура книги — отказ. Предпросмотр: что исчезнет (данные с числом непустых ячеек, диаграммы, сводные, таблицы), какие формулы станут `#ССЫЛКА!` (обычные ссылки, имя внутри текста, формулы с именованными диапазонами листа), какие имена сломаются, отсутствие отмены и последняя резервная копия. Исполнение: лист не менялся после предпросмотра, затем удаление и сверка — лист исчез, новых ошибок ссылок в книге не больше предсказанных; больше — `applied` с числами. Отмены нет; инструкция модели требует сначала предложить резервную копию.

**Найден и исправлен дефект.** Первый живой прогон показал: `rename_sheet` прошёл без карточки — в схеме он был помечен как неразрушающий, а такие инструменты выполняются без подтверждения. Переименование ломает формулы с INDIRECT, поэтому подтверждение обязательно. Исправлено; добавлен защитный тест: любой изменяющий инструмент обязан проходить через карточку.

**Проверки.** `sheetImpact.test.ts` — 3 теста (обычная ссылка, имя в кавычках, ссылка внутри текста; именованный диапазон как целое слово; перечень влияния); `sheetOps.test.ts` — 7 тестов на макете книги, ведущем себя как Excel из замера (переименование: перечень и сверка значений, отмена, занятое и то же имя, переименование в промежутке; удаление: перечень, совпадение новых ошибок с предсказанием, непредсказанная ошибка — `applied`, последний видимый лист, правка после предпросмотра); `workbookEvents.test.ts` — тест через настоящие обработчики монитора: переименование и добавление листа историю отмены не чистят, удаление — чистит; `toolSchemas.test.ts` — каждый изменяющий инструмент подтверждается. `npm run check`: 354 клиентских, 46 серверных, сборка.

**В Excel** (сборка `2026-09-24 06:13`; листы `Э7Лист` с данными и `Э7Ссылки` с `=Э7Лист!A1*2`, `=INDIRECT("Э7Лист!A1")`, `=Э7Ставка+1` и текстом «см. лист Э7Лист»; имя `Э7Ставка` на `Э7Лист!A2`):

```
прошла  7.3.1 переименование: ссылки переписаны, значения те же, INDIRECT назван
        ='Э7Лист новый'!A1*2 → 20; =Э7Ставка+1 → 21; =INDIRECT("Э7Лист!A1") → #ССЫЛКА! (назван в brokenLiteralFormulas)
прошла  7.3.1 отмена вернула прежнее имя            =Э7Лист!A1*2 → 20, INDIRECT снова → 10
прошла  7.3.1 удаление: лист исчез, сломанные формулы предсказаны и названы
        ошибок ссылок 0 → 3; названы A1, A2, A3 и имя Э7Ставка
```

## 7.3.1. Копирование листа — не выдано

24 сентября 2026 года замер `worksheet.copy` (ExcelApi 1.10; машина заявляет поддержку 1.10, структура книги не защищена): на этой книге и Office 16.0.14334 вызов падает с `GeneralException` в `Worksheet.copy` при любом варианте — `After`, `End`, лист с кириллическим и латинским именем, пустой лист, лист без объектов («Курс»). Новых листов после сбоя не остаётся. План запрещает подменять действие похожим — копия одного содержимого без диаграмм, сводных и настроек не является копией листа, — поэтому инструмент копирования модели не выдаётся, и инструкция по-прежнему говорит, что копировать листы агент не умеет. Вернуться — на другой книге или сборке Excel: сначала выяснить, от чего зависит отказ.

## 7.3.2. Вставка и удаление столбцов

Выполнено 24 сентября 2026 года.

**Замер до работ** (лист с данными A1:E3 и формулами в H): удаление столбца C превратило `=C2*10` и `=SUM(C:C)` в `#ССЫЛКА!`, молча сузило `SUM(B:D)` до `B:C`, `SUM(B2:C2)` до `B2:B2`, `SUM(A2:E2)` до `A2:D2`; `SUM(2:2)` — целая строка — не задета. Вставка внутрь диапазона его расширила (`B:C` → `B:D`), вставка вплотную за данными — нет (`SUM(A2:E2)` не охватил новый столбец F, ошибки нет).

**Сделано.** Разбор ссылок (`rowOps.ts`) понимает ссылки на целые столбцы (`C:C`, `$B:$D`, `Лист!C:C`) и целые строки (`2:2`) и считает последствия вдоль оси: целые столбцы не задеваются операциями со строками, целые строки — операциями со столбцами. Это улучшило и строки этапа 6: `SUM(2:2)` при удалении строки 2 теперь называется сломанной, раньше такие ссылки не разбирались. Сбор рисков (`collectRowRisks`) обобщён на ось, отдельной копии для столбцов нет. Новые `insert_columns` и `delete_columns` (`columnPlans.ts`) — по правилам строк этапа 6: столбец буквами, до 100 за операцию; защищённый лист и полоса через таблицу Excel — отказ до карточки; предпросмотр — содержимое удаляемой полосы в пределах занятых строк, число непустых ячеек, формулы книги, которые сломаются, сузятся или не охватят новые столбцы, объединения, непроверенные листы, отсутствие отмены; исполнение — полоса не менялась после предпросмотра, затем операция, обход книги и сверка ошибок ссылок: новых больше предсказанных — `applied`. История отмены очищается, как у строк.

**Проверки.** `rowOps.test.ts` — 4 новых теста на примерах замера (целые столбцы и строки как ссылки; удаление столбца C — сломано, сужено, не задето; вставка вплотную — не охвачено, внутрь — охвачено; разбор букв столбца). `npm run check`: 358 клиентских, 46 серверных, сборка.

**В Excel** (сборка `2026-09-24 06:27`, лист `Э7С`):

```
прошла  7.3.2 удаление столбца C: сломанные и суженные формулы предсказаны, ошибки сверены
        предсказано: H1 broken (SUM(C:C)), H2 shrunk, H3 shrunk, H4 broken (C2*10), H5 shrunk; ошибок ссылок 0 → 2
        после: =SUM(#REF!), =SUM(B:C), =SUM(B2:B2), =#REF!*10, =SUM(A2:D2), =SUM(2:2) — как в замере
прошла  7.3.2 вставка вплотную за данными: итог, который не охватит новый столбец, назван   G5 =SUM(A2:D2): missed
прошла  7.3.2 столбец через таблицу Excel — отказ до карточки
```

## 7.3.3. Группировка строк и столбцов

Выполнено 24 сентября 2026 года.

**Замер до работ** (ExcelApi 1.10): свернуть несгруппированные строки нельзя — ничего не скрывается; после группировки свёртка скрывает строки; разворот той же полосы (`showGroupDetails`) строки обратно **не показывает**, а после разгруппировки они так и остаются скрытыми; у столбцов разворот сработал только с захватом столбца итога. Вложенная группа сворачивается отдельно. Уровни группировки API не отдаёт.

**Сделано.** `group_rows_columns` (`outlinePlans.ts`): целые строки «3:5» или столбцы «C:D», до 1000; `collapse` — оставить свёрнутой. Это настоящая структура Excel, а не скрытие, — так и в инструкции модели. Предпросмотр: видимость полосы сейчас, как будет проверено, что прежние уровни не видны. Исполнение: видимость не менялась после предпросмотра; группировка; **группа доказывается свёрткой** — ранее видимые строки должны скрыться; затем видимость возвращается построчно ровно к прежней (или остаётся свёрнутой по просьбе) и сверяется. Если все строки были скрыты ещё до операции, доказать группу нечем — итог `applied` с пометкой «не проверена». Отмена снимает один уровень с той же полосы и возвращает прежнюю видимость — без этого разгруппировка оставила бы строки скрытыми. Снимать чужие группы агент не умеет, и это сказано.

**Проверки.** `outlinePlans.test.ts` — 8 тестов на макете, ведущем себя как Excel из замера (полоса; группа доказана, видимость прежняя при заранее скрытой строке; свёрнутая по просьбе; несозданная группа поймана свёрткой; все строки скрыты заранее — «не проверена»; отмена возвращает видимость, хотя разгруппировка сама бы не вернула; правка видимости после предпросмотра). `npm run check`: 366 клиентских, 46 серверных, сборка.

**В Excel** (сборка `2026-09-24 06:39`, лист `Э7Г`, строка 4 скрыта заранее):

```
прошла  7.3.3 группа строк доказана свёрткой, видимость прежняя (строка 4 так и скрыта)
прошла  7.3.3 группа столбцов свёрнута по просьбе            C, D скрыты; B, E видны
прошла  7.3.3 отмена сняла группу столбцов и вернула видимость
```

## 7.3.4. Сводная на новом листе

Выполнено 24 сентября 2026 года. `create_pivot_table` принимает `newSheet` — имя нового листа: имя проверяется правилами Excel при подготовке (занятое — отказ со свободным вариантом), лист создаётся при исполнении, сводная встаёт в A1 и сверяется как обычно. Если сводная не построилась, созданный под неё пустой лист убирается. Отмена удаляет сводную, а затем лист — если на нём больше ничего нет; иначе лист остаётся, там уже чужая работа. `newSheet` не сочетается с `destSheet` и `destAddress`. Модели сказано: для «сводной на новом листе» отдельный `create_sheet` не нужен.

**Найден и исправлен дефект.** Первый живой прогон отказал: «newSheet не сочетается с destSheet», хотя `destSheet` не передавался. Нормализатор аргументов (`resolveToolArgs`) всегда подставлял лист назначения равным листу источника; теперь при `newSheet` не подставляет. Тест закрепляет оба случая.

**Проверки.** `pivotPlans.test.ts` — 2 новых теста (план на новом листе, занятое имя, несочетаемость; нормализатор). `npm run check`: 368 клиентских, 46 серверных, сборка.

**В Excel** (сборка `2026-09-24 06:44`):

```
прошла  7.3.4 сводная на новом листе одной операцией
        'Э7 Сводная'!A1:B5: Казань 1500, Москва 1350, Омск 2800, общий итог 5650 — verified
прошла  7.3.4 отмена убрала сводную и пустой лист
```

Срез 7.3 закрыт: переименование и удаление листов, столбцы, группировка, сводная на новом листе — проверены в Excel; копирование листа не выдано из-за отказа `worksheet.copy` на этой машине.

## 7.4.2. Правила проверки ввода

Выполнено 24 сентября 2026 года.

**Замер до работ** (ExcelApi 1.8–1.9): новое правило ставится поверх прежнего без очистки; область с разными правилами — тип `Inconsistent`; уже введённые неподходящие значения Excel называет сам (`getInvalidCellsOrNullObject`): список «Новая,В работе,Закрыта» отбраковал «Отменена» и «в работе» (регистр важен), пустое прошло; число > 0 — −5, 0 и «abc»; дату правило принимает в ISO, а отдаёт как `1/1/2026`.

**Сделано.** `set_data_validation` (`validationPlans.ts`): список (без запятых внутри значений, до 255 знаков), целое число, число, дата (ГГГГ-ММ-ДД) с оператором. Предпросмотр: правило, прежнее правило области (или «разные правила — заменятся»), оценка панели — какие уже введённые значения правилу не соответствуют. Исполнение: прежнее правило не менялось после предпросмотра; установка; обратное чтение и сверка по смыслу (дата — как дата, не как текст); нарушителей называет сам Excel, и ответ говорит, что правило их не исправляет. Отмена возвращает прежнее правило или снимает правило; если прежде были разные правила, отмены нет — одним правилом их не вернуть, и карточка это говорит.

**Проверки.** `validationPlans.test.ts` — 9 тестов (проверка запроса; нарушители по правилам замера; сверка по смыслу, в том числе дата в американском виде; установка и нарушители от Excel; правило, которое Excel не принял; отмена к прежнему и к пустому; разные правила — без отмены; правка после предпросмотра). `npm run check`: 377 клиентских, 46 серверных, сборка.

**В Excel** (сборка `2026-09-24 06:48`, лист `Э7П`):

```
прошла  7.4.2 список: правило стоит, нарушители названы самим Excel      A4 «Отменена», A5 «в работе»
прошла  7.4.2 число > 0: нарушители −5, 0, «abc»                         B3, B5, B6
прошла  7.4.2 дата в 2026 году: сверена по смыслу, нарушитель C4
прошла  7.4.2 отмена сняла правило дат
```
