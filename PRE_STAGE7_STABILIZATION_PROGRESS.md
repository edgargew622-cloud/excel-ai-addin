# Стабилизация перед этапом 7 — отчёт о работах

Отчёт по `PRE_STAGE7_STABILIZATION_PLAN.md`. Ведётся по ходу работ; статусы означают ровно то, что написано, и проверки в Excel не считаются пройденными без фактического прогона пользователем.

## Порядок работ

Согласован с автором плана 20 сентября 2026 года после обсуждения: **S0 → S5 → S1 → S2 → основная часть S4 → приёмка**. S3 и S6 ведутся параллельно независимым задачам этапа 7 при условии, что непроверенное не объявляется проверенным; S3.1 закрывается до использования сводных как основания выводов, вопрос рамки — до расширения оформления.

Принятые на том же обсуждении уточнения к способу исправления:

- **S1** делается через `formulasR1C1` вместо собственного разбора формул. Автор плана верно указал, что свойство читается у ячейки и не преобразует произвольную строку, поэтому путь двухшаговый: запись первой ячейки → `sync` → чтение её R1C1 → присваивание всей области → сверка. Содержимое первой ячейки снимается до записи и возвращается при отказе на втором шаге; «вернули как было» даёт `failed_before_write`, потеря ответа после отправки — `unknown`. Временная ячейка не используется: это изменение книги со своими гарантиями.
- **S3.1** делается через табличный макет сводной. `repeatAllItemLabels` не используется: он поднял бы требование инструмента с ExcelApi 1.8 до 1.13. Пустая родительская ячейка в табличном макете означает «та же группа, что выше», и полный ключ строки восстанавливается переносом последней непустой подписи вниз по столбцу.

| Шаг | Статус |
| --- | --- |
| S0 | выполнен |
| S5 | выполнен; проверено в Chromium без Excel, дефект воспроизведён на прежней сборке; проверка отмены с задачей в Excel не выполнялась |
| S1 | не начат |
| S2 | не начат |
| S4 | не начат |
| S3 | отложен (параллельно этапу 7) |
| S6 | отложен (параллельно этапу 7) |
| S7 | не начат; требует Excel |
| S8 | не начат |

## S0. Исходная точка

Зафиксировано 21 сентября 2026 года.

**Исходники.** HEAD — `c7470fd` «Новый лист не стирает историю отмены». Рабочее дерево чистое. База аудита из плана — `93043bb`; после неё добавлены два коммита, оба по просьбе пользователя и до начала стабилизации:

- `671f508` — `create_sheet`: агент не умел создавать листы, из-за чего сводная требовала существующий лист назначения.
- `c7470fd` — исправление, найденное проверкой в Excel: монитор чистил историю отмены на добавление листа, и созданный лист нельзя было отменить.

**Пользовательские изменения сохранены.** На момент составления плана в дереве был изменён `STAGE7_PLAN.md`; эти правки вошли в коммит `671f508` без переписывания. Сам `PRE_STAGE7_STABILIZATION_PLAN.md` тоже сохранён как есть.

**Состав инструментов.** 23 инструмента, из них изменяющих — 14 (в плане указано 13; добавился `create_sheet`). Все 14 есть в реестре планов, это проверяется тестом.

**Автоматические проверки.** `npm run check`: 240 клиентских тестов, 46 серверных, сборка. Все зелёные. Это не заменяет проверок в Excel.

**Выпуск и сборка.** Запущен выпуск `20260920T225744Z-8a573808`, отметка сборки панели — `2026-09-20 22:57`. Какая сборка загружена в Excel у пользователя, отсюда не видно: панель показывает свою отметку внизу и предупреждает, если на сервере новее. Перед каждой живой проверкой это сверяется.

**Повторённые воспроизведения.** Оба дефекта из плана воспроизведены независимо, на текущих исходниках:

```
S1   shiftFormula(…, 1, 0)
     =Q1!A1          → =Q2!A2          (ожидалось =Q1!A2)
     ='Q1 2026'!A1   → ='Q2 2026'!A2   (имя листа изменено)
     =SUM(Sales[Q1]) → =SUM(Sales[Q2]) (имя столбца таблицы изменено)

S3.1 expectPivot(City+Status, sum Amount) против макета
     [["Rows","Sum"],["A",30],["Open",999],["Closed",999],["Grand Total",30]]
     pivotMismatches → []   (вложенные итоги не проверяются)
```

Остальные пункты плана на этом шаге не перепроверялись: они помечены автором как «выявлено по коду» и будут воспроизведены тестом непосредственно перед исправлением, как требует раздел 2 плана.

**Книги для опытов.** Разрушительные проверки ведутся на копиях эталонов из `scripts/create-*-workbook.py`, а не на рабочей книге пользователя. Эталоны пересоздаются скриптом перед прогоном.

**Ограничения этого шага.** Ни одна проверка в Excel на S0 не выполнялась: шаг состоит только из фиксации состояния.

## S5. Один активный цикл изменения книги

Выполнено 21 сентября 2026 года.

**Воспроизведение.** Дефект подтверждается прямо в коде панели: `send()` проверял состояние React `busy`, но между нажатием и первым `await` состояние ещё не обновлено, и второе нажатие проходило ту же проверку. Отмена жила отдельно: её кнопка учитывала `busy`, но сама `undo()` замка не брала и могла выполниться посреди записи.

**Исправление.** Добавлен `src/taskpane/workbookLock.ts` — замок с одним владельцем: `task` или `undo`. Захват синхронный, до первого `await`; освобождение в `finally`, в том числе при синхронном отказе работы. Освобождение идёт по билету, поэтому опоздавший обработчик прежней операции не снимает чужой замок. Панель подписана на смену владельца и по нему блокирует ввод; кнопка «Остановить» показывается только во время задачи, а не во время отмены. Отказ по занятости объясняет, чем именно занята панель.

**Проверки.** `src/taskpane/workbookLock.test.ts`, 6 тестов: два нажатия в одном такте запускают одну задачу; отмена и задача исключают друг друга в обе стороны; отказ работы — синхронный и асинхронный — освобождает замок; опоздавшее освобождение не снимает чужой; подписка и отписка. Отдельный тест читает исходник панели и требует, чтобы отправка и отмена шли именно через этот замок, а прежнего состояния `busy` не осталось: план прямо запрещает засчитывать тест флага, которым панель не пользуется.

`npm run check`: 246 клиентских тестов, 46 серверных, сборка — зелёные.

**Проверка настоящей панели без Excel.** Руками гонку не поймать: после первого нажатия кнопка «Отправить» сменяется на «Остановить» быстрее, чем человек нажимает второй раз, — пользователь это и заметил. Поэтому добавлен `scripts/panel-harness.mjs`: он запускает Chrome без окна, грузит собранную панель прямо из `releases/<выпуск>/panel`, подменяет `office.js` поддельным Excel, а запросы к `/api/chat` задерживает, отвечает на них или обрывает сам через протокол DevTools. Нажатия и клавиши — настоящие события браузера; Chromium — тот же движок, что у WebView2 в Excel. Зависимостей скрипт не добавляет.

Прогон 23 сентября 2026 года, одна и та же проверка на сборке до исправления и после:

```
== 20260920T225744Z-8a573808  (до S5)
  ПРОВАЛ  два клика в одном такте: запросов к модели: 2, просьба в ленте: 2 раз
  прошла  зажатый Enter: запросов к модели: 1, просьба в ленте: 1 раз
  прошла  панель заблокирована во время задачи
  прошла  после остановки можно работать дальше
  прошла  после ошибки сети панель свободна

== 20260920T230447Z-8a573808  (после S5)
  прошла  два клика в одном такте: запросов к модели: 1, просьба в ленте: 1 раз
  прошла  зажатый Enter: запросов к модели: 1, просьба в ленте: 1 раз
  прошла  панель заблокирована во время задачи: поле ввода выключено, «Остановить»: есть, «Отменить»: выключена
  прошла  после остановки можно работать дальше: новых запросов к модели: 1
  прошла  после ошибки сети панель свободна: поле ввода включено, «Остановить»: нет
```

Два клика в одном такте — это и есть дефект S5: на прежней сборке они отправляли два запроса к модели, то есть запускали два цикла изменения книги; на новой — один. Зажатый Enter на прежней сборке дефекта не показал: между событиями клавиатуры, отправленными через протокол, React успевает перерисовать панель. Это ограничение способа воспроизведения, а не довод, что гонки через клавиатуру не бывает.

**Не выполнено.** Конкуренция отмены с задачей в настоящем панельном пути: поддельный Excel скрипта не даёт отменяемых операций, поэтому кнопка «Отменить» в прогоне всё время выключена. Эта часть покрыта модульными тестами замка, но не прогоном панели. Проверок в самом Excel не было; пункт 7 таблицы S7 остаётся открытым в части отмены.
